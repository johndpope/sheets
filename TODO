
( ) Finish TODO LIST


Implementation:

( ) Change File Name
 L ( ) Make sure files sync correctly after changes
 L ( ) Autocomplete composer names
 L ( ) Auto naming

( ) Check Internet connection

( ) Delete Files

( ) Synchronization
 L ( ) button (manual)
 L ( ) automatic

( ) Settings
 L ( ) Sync Priority
 L ( ) Change Main Folder Name
 L ( ) custom Auto naming convention

( ) Folder structure

( ) File display shelf

( ) Sort Files
 L ( ) Name
 L ( ) Composer
 L ( ) musical Form
 L ( ) tempo

( ) Search for files

( ) Webview for downloading Sheet in App
( ) Choose composer picture from internet



