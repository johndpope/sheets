
Implementation:

( ) Change File Name
 L ( ) Make sure files sync correctly after changes
 L ( ) Autocomplete metadata info
 L ( ) Auto naming
 L ( ) Check double filenames

(X) Check Internet connection

( ) Delete Files

( ) Synchronization
 L ( ) Import files setting 
    L ( ) Parse file metadata from filename
 L ( ) button (manual)

( ) Settings
 L (X) Setup Google Drive Sync
 L ( ) custom Auto naming convention

( ) File display shelf

( ) Filter Files
 L ( ) Name
 L ( ) Composer
 L ( ) musical Form
 L ( ) tempo

( ) Search for files

( ) Webview for downloading Sheet in App
( ) Choose composer picture from internet



