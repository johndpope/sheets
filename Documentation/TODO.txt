
Implementation:

(X) Change File Name
 L (X) Make sure files sync correctly after changes
 L (X) Autocomplete metadata info
 L (X) Auto naming
 L (X) Check double filenames

(X) Check Internet connection

(X) Delete Files

(X) Synchronization
 L (X) Import files setting 
    L (X) Parse file metadata from filename
 L (X) button (manual)

( ) Settings
 L (X) Setup Google Drive Sync
 L ( ) custom Auto naming convention

(X) File display shelf

(X) Filter Files
 L (X) Name
 L (X) Composer
 L (X) musical Form
 L (X) tempo

(X) Search for files

(X) Webview for downloading Sheet in App
( ) Choose composer picture from internet



